def largest_odd(x, y, z):
    
    '''
    Write a program that examines 3 variables: x, y, z 
    and prints the largest odd number among them.
    If none of them are odd, it print the smallest value
    '''
    
    a, b, c = 0,  0, 0
    
    for i in (x, y, z):
        if i >= math.ceil(avg) and i%2 > 0:
            odd = i


values = [x, y, z]
seq = [max(values), i, min(values) for i in values but not in [min(values), max(values)]]




sor = sorted(x, y, z, reverse = True)





largest_odd(3, 6, 4)

largest_odd(3, 6, 8)

largest_odd(2, 4, 3)

